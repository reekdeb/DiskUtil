[package]
name = "diskutil"
version = "0.1.2"
edition = "2024"

[dependencies]

clap = { version = "4.6.1", features = ["derive"] }
glob = "0.3"
regex = "1"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
filetime = "0.2"
